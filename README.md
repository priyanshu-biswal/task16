# task16
## How to Run

1. Clone the repository:

2. Navigate to the project folder:

   ```bash
   cd your-repository
   ```


3. Open the `index.html` file in a web browser.

4. If the project uses JavaScript, open the browser's Developer Console to view the output:

   * **Windows/Linux:** `F12` or `Ctrl + Shift + J`
   * **macOS:** `Cmd + Option + J`
